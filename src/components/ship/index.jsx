import React from 'react';
export class Ship extends React.Component {
    constructor(props) {
        super(props);
    }
    render(props){
        // switch(props.type){
        //     case shipType.five_one:
        //         return <React.Fragment />;
        // }
        return <React.Fragment />;
    }
}